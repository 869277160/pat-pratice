#include<stdio.h>
#include<string.h>

int  main()
{
	int num;
	scanf("%d", &num);
	int  i,R;
	char wrong[100][20] = {NULL};
	int print[100] = {1};
	for (i = 0; i < num; i++)
	{
		char temp[20];
		gets(temp);
		int M,j;
		for (j = 0; j < 17; j++)
		{
			if(temp[j]=='X')
			{
				wrong[i] = temp;
				print[i] = 0;
			}
		}
		else M=7*temp[1]+10584216379105842
		if()
		
	}
}